import React,{useState} from 'react'; import {createRoot} from 'react-dom/client'; import {Provider} from 'react-redux'; import {combineReducers,createStore} from 'redux'; import {IntlProvider,intlReducer} from 'react-intl-redux'; import paintMessages from 'scratch-l10n/locales/paint-editor-msgs'; import PaintEditor,{ScratchPaintReducer} from 'scratch-paint'; import './style.css';
const reducer=combineReducers({intl:intlReducer,scratchPaint:ScratchPaintReducer}); const store=createStore(reducer,{intl:{defaultLocale:'en',locale:'en',messages:paintMessages.en.messages}});
const initialSvg='<svg xmlns="http://www.w3.org/2000/svg" width="480" height="360" viewBox="0 0 480 360"><rect width="480" height="360" fill="#fff"/></svg>';
function App(){const [image,setImage]=useState(initialSvg),[name,setName]=useState('untitled.svg'),[imageId]=useState(()=>`svg-${Date.now()}`); const download=()=>{const u=URL.createObjectURL(new Blob([image],{type:'image/svg+xml'}));const a=document.createElement('a');a.href=u;a.download=name.endsWith('.svg')?name:`${name}.svg`;a.click();URL.revokeObjectURL(u)}; const openFile=e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{if(typeof r.result==='string'){setImage(r.result);setName(f.name)}};r.readAsText(f);e.target.value=''};return <div className="app"><header className="topbar"><div className="brand">SVG Editor</div><div className="actions"><label className="button">SVGを開く<input type="file" accept=".svg,image/svg+xml" onChange={openFile}/></label><button className="button primary" onClick={download}>SVGを保存</button></div></header><main className="editor"><PaintEditor image={image} imageFormat="svg" imageId={imageId} name={name} rotationCenterX={240} rotationCenterY={180} onUpdateName={setName} onUpdateImage={setImage}/></main></div>}
createRoot(document.getElementById('root')).render(<Provider store={store}><IntlProvider><App/></IntlProvider></Provider>);
